class Confidence:

    def __init__(self, data):
        self.data = data
        self.actionComplete = False
        self.distance = 0

    def confidence_execution(self, action):
        while True:
            if self.actionComplete:
                self.distance = 1

    def nearest_neighbor(self):
        return 0

    def distance_threshold(self):
        return 0

    def confidence_threshold(self):
        return 0
